1. 跑 pytest
      ↓
2. 全部通过？ → 是 → 结束
      ↓ 否
3. 从输出里找出 tests/ 下失败的 .py 文件
      ↓
4. 把失败输出 + 测试源码发给 OpenAI
      ↓
5. AI 返回修复后的完整代码
      ↓
6. 验证代码语法（ast.parse）
      ↓
7. 写回文件
      ↓
8. 重跑 pytest
      ↓
9. 通过？ → 结束（最多重试 N 轮）